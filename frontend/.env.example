# Nexus Frontend — Variables de entorno
# Copia este archivo a .env y ajusta la URL. Nunca commitees .env
# Requerida y validada en src/api/client.js (RF-3). Debe ser absoluta sin barra final.

# Local
VITE_API_URL=http://localhost:8000

# Producción (ejemplo)
# VITE_API_URL=https://api.tu-dominio.com
