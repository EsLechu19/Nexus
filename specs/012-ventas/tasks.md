# Tasks 012 — Registro de Ventas

> Orden de dependencia. Cada tarea 20-30 min. Respetar `docs/constitution.md:1-6` y `AGENTS.md:16-32`. Reutiliza `003-movimientos` sin reimplementar descuento y exige `Bearer` como `011`. `pytest -v` 100% verde al cierre. `RF` marcados por tarea.

## Fase 1 — Modelo, esquema y validación sintáctica

- [x] **T01 — Crear modelos `Venta` y `VentaItem` y migración Alembic** — RF-1, RF-2, RNF-3 — Hecho cuando: `app/models/venta.py` con `Venta` → `ventas` (`id PK`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`, `cliente_nombre VARCHAR(100) NOT NULL CHECK 2-100`, `cliente_email VARCHAR(254) NULL`, `total NUMERIC(12,2) NOT NULL`) y `app/models/venta_item.py` con `VentaItem` → `venta_items` (`id PK`, `venta_id FK ventas.id RESTRICT`, `producto_id FK productos.id RESTRICT`, `cantidad INTEGER CHECK 1..1_000_000`, `precio_unitario NUMERIC(12,2) CHECK 0..1_000_000`, `subtotal NUMERIC(12,2)`, `movimiento_id FK movimientos_inventario.id UNIQUE NULL->NOT NULL`) + índices `ix_ventas_created_at`, `ix_venta_items_venta_id`; `alembic revision --autogenerate -m "crea ventas 012"` y `alembic upgrade head`/`downgrade -1`+`upgrade` sin errores; `ruff`/`mypy` verdes.

- [x] **T02 — Crear schemas Pydantic de venta con orden de validación** — RF-1, RNF-6 — Hecho cuando: `app/schemas/venta.py` con `VentaCreate` (`cliente: {nombre, email?}`, `items: list[VentaItemCreate] 1-20`), `VentaItemCreate` (`producto_codigo: str` normaliza `trim+upper` antes de regex `^[A-Z0-9_-]{3,20}$`, `cantidad: StrictInt 1..1_000_000` rechaza `2.00`, `precio_unitario: Decimal max_digits 12 decimal_places 2 ge0 le1000000` rechaza `>2` decimales y `float`, `model_validator` detecta `SKU` duplicado tras normalización), `VentaResponse`/`VentaListResponse` con `id`, `created_at` (`ISO 8601 Z`), `cliente`, `items` con `subtotal`, `total`, `movimientos_ids`; `extra="ignore"` para `total`/`subtotal`/`created_at`/`id` enviados (se ignoran sin 422); `mypy` verde.

## Fase 2 — Service de ventas y transacción atómica

- [x] **T03 — Implementar `venta_service` validación propia sin BD vs con BD (orden 422→404→400)** — RF-1, RF-2 — Hecho cuando: `app/services/venta_service.py` expone `validar_formato(items, cliente)` que sin BD rechaza `422` para `[]`/`>20`/duplicado/`producto_codigo` formato/`cantidad` no entera/`precio` `>2` decimales/`cliente nombre` 2-100/`email ""`→422 y `null`/ausente→sin email; expone `validar_existencia_y_activo` que con BD hace `404` si no existe y `400` si `inactivo`; `pytest -v -k test_venta_validacion` verde.

- [x] **T04 — Implementar `venta_service` lectura de stock propia y cálculo Decimal** — RF-1, RF-2, RNF-6 — Hecho cuando: `venta_service` expone `calcular_stock_actual` reutilizando `SELECT FOR UPDATE` del producto (como `003`) y calcula `subtotal = cantidad*precio_unitario` y `total = sum(subtotal)` con `Decimal` (nunca `float`); valida `cantidad ≤ stock_actual` y si falla → `400` específico `Stock insuficiente para X: disponible N, solicitado M`; `pytest -v -k test_venta_stock` verde.

- [x] **T05 — Implementar `venta_service.registrar_venta` transacción atómica con invocación a 003** — RF-2, RNF-2, RNF-3 — Hecho cuando: `registrar_venta(db, datos)` abre una sola transacción `BEGIN`, ejecuta orden completo `422`→`404`→`400` con validación propia leyendo stock con `SELECT FOR UPDATE`, y solo si todo valida invoca `N` veces `movimiento_service.registrar_salida` (ya autorizado) para crear `salida` por ítem; `INSERT` en `ventas` y `venta_items` con `movimiento_id`; `COMMIT` o `ROLLBACK` total si cualquier ítem falla; `stock_actual` de otros ítems sin cambios verificado; no expone `UPDATE`/`DELETE` sobre ventas; `pytest -v -k test_venta_atomicidad` verde.

## Fase 3 — Routers protegidos

- [x] **T06 — Crear router `POST /api/v1/ventas` protegido** — RF-1, RF-2, RNF-5 — Hecho cuando: `app/routers/ventas.py` expone `POST /api/v1/ventas` con `dependencies=[Depends(get_current_user)]` (Bearer antes de BD), delega a `venta_service.registrar_venta`; sin `Bearer` → `401` antes de `SELECT`; con `Bearer` válido y carrito válido → `201` con `id`, `created_at` `Z`, `cliente` normalizado, `items` con `subtotal`, `total` derivado y `movimientos_ids`; `422` para `[]`/`>20`/duplicado/`2.00`/`10.123`, `404` no encontrado, `400` inactivo/stock con detalle; `total` enviado ignorado; `app/main.py` registra `ventas.router`; `pytest -v -k test_ventas_post` verde.

- [x] **T07 — Crear routers `GET /api/v1/ventas` y `GET /api/v1/ventas/{id}` protegidos** — RF-3, RF-4, RNF-5 — Hecho cuando: `app/routers/ventas.py` añade `GET /api/v1/ventas` (sin barra final, orden `created_at DESC, id DESC`, `[]` si vacío) y `GET /api/v1/ventas/{id}` (404 si no existe) ambos con `Depends(get_current_user)` antes de BD; sin `Bearer` → `401` antes de `SELECT`; con token → `200` con `movimientos_ids` que existen en `GET /movimientos`; no expone `PUT`/`PATCH`/`DELETE` (`405`); `pytest -v -k test_ventas_get` verde.

## Fase 4 — Tests y cierre

- [x] **T08 — Tests unitarios `venta_service` (validación, stock, Decimal, atomicidad)** — RF-1, RF-2, RNF-6 — Hecho cuando: `tests/test_venta_service.py` (mock `Session`/`sqlite`) cubre: `cliente` `""`→422 `null`→sin email, `items` `[]`/`21`→422, duplicado tras normalización→422, `cantidad` `2.00`/`"2"`→422, `precio` `10.123`→422 `10.5`→201, `total` enviado ignorado, `404` no existe, `400` inactivo/stock con `disponible/solicitado`, `Decimal` exacto `total==sum`, `rollback` total sin movimientos si un ítem falla, concurrencia `SELECT FOR UPDATE` una `201` otra `400`; `pytest -v -k test_venta_service` verde.

- [x] **T09 — Tests integración `ventas` (endpoints con Bearer)** — RF-1..RF-4, RNF-5 — Hecho cuando: `tests/test_ventas.py` (`TestClient` con `Bearer` de `conftest.auth_headers`) cubre: `POST /ventas` `201` con `created_at` `Z` y `movimientos_ids` 2 IDs, `422` para `[]`/`duplicado`/`2.00`, `404` no existe, `400` stock con detalle, `total` manipulado ignorado, `GET /ventas` `200` orden `created_at DESC`, `GET /ventas/{id}` `200` con `movimientos_ids` en `GET /movimientos`, `401` sin `Bearer` para los 3 endpoints, `404` id no existe; `pytest -v -k test_ventas` verde.

- [x] **T10 — Verificación final de no regresión y lint** — Todos los RF, RNF-7 — Hecho cuando: `pytest -v` 100% verde (suites `003` y `011` con `Bearer` + `012` nuevas); `ruff check .` sin errores; `mypy app` sin errores; `alembic upgrade head` y `downgrade -1`+`upgrade` sin pérdida; `openapi.json` muestra 3 endpoints `POST /ventas`, `GET /ventas`, `GET /ventas/{id}` con `security HTTPBearer` y sin `PUT`/`PATCH`/`DELETE`; `GET /ventas` sin `Bearer` → `401`.