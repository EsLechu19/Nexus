# Tasks 013 — Pantalla de Autenticación Frontend

> Orden de dependencia. Cada tarea 20-30 min. Respetar `frontend/docs/constitution.md:1-6` y `frontend/AGENTS.md:16-32`. Reutiliza `005-frontend-base` (cliente `src/api/client.js` centralizado, layout) y consume `011-autenticacion` (`POST /api/v1/auth/login` Bearer 8h). Token solo en memoria, `401` global con exclusión de login, guard antes que `404`. `npm run test`/`npm run lint` verde al cierre. `RF` marcados por tarea.

## Fase 1 — Estado de sesión y cliente API

- [x] **T01 — Crear `AuthContext` con token solo en memoria** — RF-2, RF-3, RF-5, RNF-1, RNF-4 — Hecho cuando: `frontend/src/context/AuthContext.jsx` expone `AuthProvider` con `token: string|null` inicial siempre `null` (sin leer `localStorage`/`sessionStorage`), `isAuthenticated`, `login(token)`, `logout()` (limpia a `null`); provee a `App`/`ProtectedRoute`/`Header`/`client.js` vía getter sin persistencia; `F5` → `null` y vuelve a `/login`; `frontend/AGENTS.md:27` sin nuevas deps; `npm run lint` sin errores.
- [x] **T02 — Extender cliente API centralizado con Bearer e interceptor 401** — RF-4, RF-5 — Hecho cuando: `frontend/src/api/client.js` (modifica existente de `005`) lee token vía `setAuthTokenGetter(() => token)` registrado por `AuthContext`, inyecta `Authorization: Bearer <token>` en cada `fetch` excepto cuando `url` termina en `/api/v1/auth/login` (nunca adjunta aunque haya token previo); intercepta `401` solo si no es login: limpia token vía `onUnauthorized`, navega a `/login` con estado `Sesión expirada, inicia sesión nuevamente` (persistente hasta interacción) y no dispara `ErrorMessage` de sección; `401` de `POST /auth/login` se propaga como `401` normal sin limpiar; otros `4xx`/`5xx`/red mantienen tipificación `005` (`validacion` sin Reintentar vs `conexion` con Reintentar); lógica vive solo en `src/api/`, `grep -r "fetch(" src/components src/pages` vacío; `npm run test` cliente con `fetch` mockeado verde.
- [x] **T03 — Crear wrapper `auth.js` para login** — RF-2 — Hecho cuando: `frontend/src/api/auth.js` expone `login({email,password})` que aplica `trim` a `email`, no añade `Bearer`, delega a `client.js` `POST /api/v1/auth/login` y devuelve `{access_token, token_type}`; no guarda token (lo devuelve al contexto); maneja `422` array (`detail[0].msg`) vs string tal cual; `npm run lint` verde.

## Fase 2 — Pantallas y guard de rutas

- [x] **T04 — Crear `pages/Login.jsx` con validación mínima y estados** — RF-1, RF-2 — Hecho cuando: `Login.jsx` muestra form `email` (`autocomplete="email"`) + `contraseña` (`autocomplete="current-password"`) + botón `Iniciar sesión`; validación local solo `Campo requerido` si vacío (no valida formato), bloquea `fetch` y muestra mensaje por campo; mientras `fetch` pendiente deshabilita inputs+botón con spinner e ignora dobles clics; `401` → `Credenciales inválidas` en form sin guardar token y sin disparar interceptor global; `422` → primer mensaje legible si array o string tal cual; red/timeout → `Error de conexión con el servidor` con `Reintentar`; éxito → `AuthContext.login(token)` y `navigate(state.from || "/productos")`; nunca muestra registro/recuperación/roles; `npm run test` con `fetch` mockeado verde.
- [x] **T05 — Crear `ProtectedRoute` y reconfigurar `App.jsx`** — RF-3 — Hecho cuando: `frontend/src/components/ProtectedRoute.jsx` consume `isAuthenticated`; si `!isAuthenticated` → `<Navigate to="/login" state={{from: location}} replace />` sin intentar `fetch` y recordando ruta; si autenticado y ruta es `/login` → `<Navigate to="/productos" replace />` (cambiar de cuenta exige logout previo); si autenticado y ruta válida → `<Outlet />`; `App.jsx` monta `AuthProvider` + `BrowserRouter` + `Routes`: `/login` público fuera de `ProtectedRoute`, resto (`/productos|proveedores|movimientos|stock|ventas`) bajo `ProtectedRoute` → `AppLayout`; `/` → `/productos`; `*` → `NotFoundPage` solo dentro de protegidas → guard se evalúa primero y `/ruta-inexistente` sin token va a `/login` no a `404`; `F5` con token perdido vuelve a `/login` recordando ruta; `npm run test` guard verde.
- [x] **T06 — Modificar layout `Header` con `Cerrar sesión` condicional** — RF-5 — Hecho cuando: `frontend/src/layout/Header.jsx` (extraído de `AppLayout`) muestra botón `Cerrar sesión` siempre visible en header solo si `isAuthenticated` (nunca en `/login`); al click llama `AuthContext.logout()` sin `fetch`, navega a `/login` con estado `Sesión cerrada correctamente` efímero 3s (mismo patrón `006-009`) y sin confirmación; doble click ignorado mientras navega; no añade `localStorage`; `npm run test` header verde.

## Fase 3 — Tests y cierre

- [x] **T07 — Tests de componente y verificación final sin regresión** — RF-1..RF-5, RNF-1..RNF-5 — Hecho cuando: tests Vitest+Testing Library con `fetch` mockeado cubren `Login` (render, `Campo requerido` sin fetch, `trim` email, `fetch` sin `Bearer`, disabled durante carga, `401`/`422` array/string/red con `Reintentar`), `ProtectedRoute` (sin token → `/login` con `from`, sin token + `*` → `/login` no `404`, con token + `/login` → `/productos`, con token → `Outlet`, `F5` → `login`), `client.js` interceptor (Bearer excepto login, `401` con Bearer → logout + `/login` `Sesión expirada` persistente, `401` login → no logout, otros `4xx`/`5xx` sin logout), `Header` (oculto sin sesión, visible con sesión, logout → `/login` `Sesión cerrada` 3s); `npm run test` 100% verde, `npm run lint` sin errores, `npm run dev` manual verifica login → `Bearer` en `movimientos`/`stock`/`ventas`, `401` expirado → `Sesión expirada`, `F5` pierde sesión y vuelve a `login` recordando `/stock`, `Cerrar sesión` funciona sin `localStorage` (inspección `Application` vacía).
