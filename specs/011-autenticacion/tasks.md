# Tasks 011 — Autenticación Básica

> Orden de dependencia. Cada tarea 20-30 min. Respetar `docs/constitution.md:1-6` y `AGENTS.md:16-32`. No añade lógica de negocio de `001-004` ni rompe `stock_actual`. Tokens y usuarios solo como en `spec.md` y `plan.md:1-11`. `pytest -v` 100% verde al cierre. `RF` marcados por tarea.

## Fase 1 — Modelo, esquema y servicio base

- [x] **T01 — Crear modelo `Usuario` y migración Alembic `usuarios`** — RF-4 — Hecho cuando: `app/models/usuario.py` existe con `Usuario` (singular) → `usuarios` con `id PK`, `email VARCHAR(254) UNIQUE NOT NULL` (normalizado `trim+lower`), `password_hash VARCHAR(72) NOT NULL`, `created_at timestamptz` + `ix_usuarios_email` único; `alembic revision --autogenerate -m "crea usuarios 011"` genera `xxxx_crea_usuarios_011.py` y `alembic upgrade head` y `downgrade -1`+`upgrade` sin errores y `psql \d usuarios` muestra `UNIQUE`; `ruff check .` y `mypy app` sin errores.

- [x] **T02 — Crear schemas Pydantic de auth** — RF-1, RF-4, RNF-4 — Hecho cuando: `app/schemas/auth.py` existe con `LoginRequest` (`email: str`, `password: str` requeridos), `LoginResponse` (`access_token: str`, `token_type: str = "bearer"`), `TokenPayload` (`sub: str`, `exp: int`); `LoginRequest` no expone `password_hash`; validación de ausencia de campo produce `422` vía Pydantic (ver T06); `mypy app` sin errores.

- [x] **T03 — Implementar `auth_service` hasheo y verificación** — RF-4, RNF-1, RNF-3 — Hecho cuando: `app/services/auth_service.py` expone `normalizar_email`, `validar_email_formato` (regex `002`), `hash_password` (`passlib+bcrypt` cost 12, salt automático) y `verificar_password`; `normalizar_email("  ADMIN@Tienda.COM ") == "admin@tienda.com"`; `hash_password("secreto123") != "secreto123"` y dos hashes del mismo claro son distintos; `verificar_password` con hash correcto → `True`, incorrecto → `False`; ningún log expone claro/hash; `pytest -v -k test_auth_service_hash` verde.

- [x] **T04 — Implementar generación y validación JWT HS256 8h** — RF-1, RF-5, RNF-2 — Hecho cuando: `auth_service` expone `crear_token(email)` que devuelve JWT `HS256` con `sub=email_normalizado` y `exp = now + 8h` (UTC), leyendo `JWT_SECRET_KEY` de env var (nunca hardcodeado); `validar_token(token)` decodifica con `PyJWT` `algorithms=["HS256"]` y verifica `exp` y firma; token recién creado decodificado tiene `sub==email` y `exp` ≈ `now+28800` ±60s; `pytest -v -k test_jwt` verde y `grep -r JWT_SECRET_KEY app --include="*.py"` no muestra valor literal.

## Fase 2 — Dependency y endpoint público

- [x] **T05 — Crear dependency `get_current_user` con orden antes de BD** — RF-2 — Hecho cuando: `app/services/deps.py` (o `app/routers/deps.py`) expone `get_current_user` que lee `Authorization` header, exige prefijo `Bearer ` case-sensitive, extrae token, llama a `validar_token` y retorna `sub`; si falta header, vacío, `Basic`, `Bearer` sin token, firma alterada o `exp` expirado → `HTTPException 401`; la función no abre `Session` ni hace `SELECT`; se verifica que un `401` en `POST /api/v1/movimientos/salidas` con `cantidad > stock` no deja lock (no se ejecuta `SELECT FOR UPDATE` — verificado por test de orden); `ruff`/`mypy` verdes.

- [x] **T06 — Crear router `POST /api/v1/auth/login` con regla de dos niveles** — RF-1, RF-3, RNF-4 — Hecho cuando: `app/routers/auth.py` expone `POST /api/v1/auth/login` público (sin `Depends`), delega a `auth_service`; `email` o `password` ausentes en body → `422` (Pydantic `missing`); cualquier otro caso (formato inválido `sinarroba`, no existe, incorrecta) → `401 {"detail": "Credenciales inválidas"}` idéntico; éxito → `200 {"access_token": "<jwt>", "token_type": "bearer"}` utilizable como `Authorization: Bearer <token>` en `GET /api/v1/productos`; respuesta nunca contiene `password` ni `password_hash`; `app/main.py` registra `auth.router` bajo `/api/v1/auth`; `pytest -v -k test_login` verde.

- [x] **T07 — Configurar Swagger/OpenAPI bearer** — RF-3 — Hecho cuando: `app/main.py` configura `HTTPBearer(bearerFormat="JWT")` y `securitySchemes: {"HTTPBearer": {"type": "http", "scheme": "bearer", "bearerFormat": "JWT"}}`; `openapi.json` muestra `security: [{"HTTPBearer": []}]` en todas las operaciones bajo `/api/v1/*` salvo `POST /auth/login`; `GET /docs` y `GET /openapi.json` responden `200` sin token pero `Try it out` sin `Authorize` en cualquier `/api/v1/*` responde `401`; verificado con `curl -s http://localhost:8000/openapi.json | grep -q securitySchemes`.

## Fase 3 — Protección retroactiva de 001-004

- [x] **T08 — Proteger routers existentes `productos|proveedores|movimientos|stock`** — RF-2, Impacto en 001-004 — Hecho cuando: `app/routers/productos.py`, `proveedores.py`, `movimientos.py`, `stock.py` añaden `dependencies=[Depends(get_current_user)]` a cada `APIRouter` (o a cada operación) sin tocar lógica interna de `services`; `GET/POST/PATCH/DELETE` bajo `/api/v1/productos`, `/proveedores`, `/movimientos/*`, `/stock` sin `Authorization` → `401`; con `Bearer` válido → `200`/`201` con misma lógica de `001-004`; `GET /api/v1/stock` con producto `inactivo` sigue `404` (no `401` por negocio); `pytest` de `001-004` con token sigue verde (ver T10).

## Fase 4 — CLI y actualización de tests

- [x] **T09 — Crear comando CLI administrativo `crear_usuario`** — RF-4, RNF-1 — Hecho cuando: `app/cli/crear_usuario.py` existe como `python -m app.cli.crear_usuario --email <email> --password <password>` (o `crear-usuario`); reutiliza `auth_service.normalizar_email` y validación `002` y `len(password)>=8`; si `email` duplicado (normalizado) → error `Email ya existe` y no crea; si `<8` → error validación; si ok → `INSERT` en `usuarios` con `email` normalizado y `password_hash` bcrypt, salida `Usuario creado: <email>` sin mostrar hash; ningún endpoint HTTP `POST /api/v1/usuarios` existe (`curl → 404`); `ruff`/`mypy` verdes.

- [x] **T10 — Actualizar setup de tests existentes 001-004 con fixture `auth_headers`** — Impacto 001-004, RNF-6 — Hecho cuando: `tests/conftest.py` (o `tests/fixtures/auth.py`) expone fixture `auth_headers` (scope `function`) que crea usuario de prueba `test_011_{uuid}@tienda.com` vía `auth_service`/`INSERT` con hash y hace `POST /auth/login` para obtener `Bearer`, retornando `{"Authorization": "Bearer <token>"}`; todos los `TestClient` de `001`/`002`/`003`/`004` modificados para enviar `headers=auth_headers` en cada `client.get/post/patch/delete`; `pytest -v` de suites `001-004` vuelve a `verde` con autenticación activa; un test de control `client.get("/api/v1/productos")` sin header → `401` confirma protección.

## Fase 5 — Tests nuevos y cierre

- [x] **T11 — Tests unitarios `auth_service` (hasheo, JWT, RNF-8)** — RF-1, RF-4, RNF-1, RNF-2, RNF-8 — Hecho cuando: `tests/test_auth_service.py` (o `tests/unit/test_auth_service.py`) cubre: `email` normalizado `  ADMIN@Tienda.COM `→`admin@tienda.com`, formato `002` y `≤254`; `hash` distinto al claro y salts distintos; `crear_token` con `sub` y `exp≈now+8h`; `validar_token` con firma alterada/`exp` pasado → `401`; `RNF-8` token emitido, borrado del usuario vía `DELETE FROM usuarios`, uso posterior hasta `exp` sigue `200`; `pytest -v -k test_auth_service` verde y no expone hash en asserts.

- [x] **T12 — Tests integración `auth` y acceso protegido** — RF-1, RF-2, RF-3, RF-5, RNF-4, RNF-8 — Hecho cuando: `tests/test_auth.py` (o `tests/integration/test_auth.py`) cubre: `POST /auth/login` `200` con `access_token`/`token_type`, `422` con `email` o `password` ausentes, `401` con `sinarroba`/`no-existe`/`errónea` idéntico y sin `password_hash` en respuesta; `GET /productos` sin `Bearer`, con `Bearer` vacío/`Basic`/`firma alterada`/`exp` expirado → `401`; con `Bearer` válido → `200`; `GET /docs` `200` sin token pero `POST /productos` vía `Try it out` sin `Authorize` → `401`; no hay `POST /logout` → `404`; `DELETE FROM usuarios` tras login y `GET /productos` con token antiguo → `200` hasta `exp`; `pytest -v -k test_auth` verde.

- [x] **T13 — Verificación final de no regresión y lint** — Todos los RF, RNF-6, RNF-7 — Hecho cuando: `pytest -v` 100% verde (suites `001-004` con fixture + `011` nuevas); `ruff check .` sin errores; `mypy app` sin errores; `alembic upgrade head` y `alembic downgrade -1`+`upgrade` sin pérdida; `curl -s http://localhost:8000/openapi.json | grep securitySchemes` muestra `HTTPBearer`; `grep -r "password_hash" app/schemas --include="*.py"` vacío (nunca expuesto); `grep -r "JWT_SECRET" app --include="*.py"` muestra solo `os.getenv` sin valor literal.
